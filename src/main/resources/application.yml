spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/healthcare
    username: healthcare
    password: healthcare
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: dev

server:
  port: 8080
